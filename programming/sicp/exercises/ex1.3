# Exercise 1.3

(define (square x)
    (* x x))

(define (sum-of-squares x y)
   (+ (square x) (square y)))

(define (sos2lg a b c)
    (cond ((and (<= a b) (<= a c)) (sum-of-squares b c))
	  ((and (<= b a) (<= b c)) (sum-of-squares b c))
          ((and (<= c a) (<= c b)) (sum-of-squares b c))))
